<%= simple_form_for @reading, id: "select-beast" do |f| %>
  <%= f.association :book, label: "Titre", input_html: {data: {controller: "tom-select"}} %>
  <%= f.input :EAN %>
  <%= f.button :submit, "Ajouter à ma bibliothèque" %>
<% end %>
